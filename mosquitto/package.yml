name: mosquitto
version: 2.0.7

scheme:
  shared:
    options:
      shared: true
  static:
    options:
      shared: false

#program:
#- name: test_package
#  location: test_package
#  executable: bin/test_package

dependencies:
  openssl:
    version: 1.1.1i
    if: options.with_tls
  libwebsockets:
    version: 4.1.6
    if: options.with_websockets
       


