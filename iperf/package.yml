name: iperf
version: '3.9'

scheme:
  shared:
    options:
      shared: true

  static:
    options:
      shared: false

program:
- name: iperf3
  location: iperf3
  executable: package/bin/iperf3
  
dependencies:
  openssl: 
    version: 1.1.1g

