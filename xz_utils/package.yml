name: xz_utils
version: 5.2.4

scheme:
  shared:
    options:
      shared: true
  static:
    options:
      shared: false
  
program:
- name: test_package
  location: test_package
  executable: bin/test_package

build-tools:
  msys2:
    version: 20190524
    if: compiler == 'Visual Studio'  
