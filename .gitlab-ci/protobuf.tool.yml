stages:
- build
- deploy

    # ---------------------------------
    #     Build
    # ---------------------------------
protobuf.tool.build@windows:
    stage: build
    tags:
    - builder
    - Windows
    - '@oss-0.1'
    artifacts:
      paths:
      - protobuf/.conan/$PROFILE/$SCHEME
      - protobuf/.epm
    script:
    - cd protobuf
    - epm --profile $PROFILE --scheme $SCHEME  create --archive .conan/$PROFILE/$SCHEME
    parallel:
      matrix:
      - SCHEME: ['static']
        PROFILE: ['vs2019']

protobuf.tool.build@linux:
    stage: build
    tags:
    - builder
    - Linux
    - '@oss-0.1'
    artifacts:
      paths:
      - protobuf/.conan/$PROFILE/$SCHEME
      - protobuf/.epm
    script:
    - cd protobuf
    - epm --profile $PROFILE --scheme $SCHEME  create --archive .conan/$PROFILE/$SCHEME
    parallel:
      matrix:
      - SCHEME: ['static']
        PROFILE: ['gcc5']
    


    # ---------------------------------
    #     deploy
    # ---------------------------------
protobuf.tool.publish@linux:
    stage: deploy
    tags:
    - '@oss-0.1'    
    - deployer
    script:
    - cd protobuf
    - epm --profile $PROFILE --scheme $SCHEME  --storage .conan/$PROFILE/$SCHEME upload
    parallel:
      matrix:
      - SCHEME: ['static']
        PROFILE: ['vs2019']

      - SCHEME: ['static']
        PROFILE: ['gcc5']
