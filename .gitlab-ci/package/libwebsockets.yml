stages:
- build
- test
- deploy

    # =================================
    # MSVC build
    # =================================
libwebsockets:MSVC@build:
  stage: build
  tags:
  - Windows
  - builder
  - workbench:oss-0.2
  artifacts:
    paths:
    - libwebsockets/.conan/$EPM_PROFILE/$EPM_SCHEME
    - libwebsockets/.epm
  script:
  - cd libwebsockets
  - epm create --archive .conan/$EPM_PROFILE/$EPM_SCHEME
  parallel:
    matrix:
    - EPM_PROFILE: ['vs2019d', 'vs2019']
      EPM_SCHEME: ['shared', 'static']

    # =========================================
    # gcc6 x86_64 build
    # =========================================
libwebsockets:gcc6@build:
  stage: build
  tags:
  - Linux
  - builder
  - docker
  - workbench:oss-0.2
  image: 172.16.0.119:8482/edgetoolkit/gcc6
  artifacts:
    paths:
    - libwebsockets/.conan/$EPM_PROFILE/$EPM_SCHEME
    - libwebsockets/.epm
  script:
  - cd libwebsockets
  - epm create --archive .conan/$EPM_PROFILE/$EPM_SCHEME
  parallel:
    matrix:
    - EPM_PROFILE: ['gcc6d', 'gcc6']
      EPM_SCHEME: ['shared', 'static']

    # =========================================
    # gcc7-armv7 armv7 build
    # =========================================
libwebsockets:gcc7-armv7@build:
  stage: build
  tags:
  - Linux
  - builder
  - docker
  - workbench:oss-0.2
  image: 172.16.0.119:8482/edgetoolkit/gcc7-armv7
  artifacts:
    paths:
    - libwebsockets/.conan/$EPM_PROFILE/$EPM_SCHEME
    - libwebsockets/.epm
  script:
  - cd libwebsockets
  - epm create --archive .conan/$EPM_PROFILE/$EPM_SCHEME
  parallel:
    matrix:
    - EPM_PROFILE: ['gcc7-armv7']
      EPM_SCHEME: ['shared', 'static']

    # =========================================
    # gcc5-armv7 armv7 build
    # =========================================
libwebsockets:gcc5-armv7@build:
  stage: build
  tags:
  - Linux
  - builder
  - docker
  - workbench:oss-0.2
  image: 172.16.0.119:8482/edgetoolkit/gcc5-armv7
  artifacts:
    paths:
    - libwebsockets/.conan/$EPM_PROFILE/$EPM_SCHEME
    - libwebsockets/.epm
  script:
  - cd libwebsockets
  - epm create --archive .conan/$EPM_PROFILE/$EPM_SCHEME
  parallel:
    matrix:
    - EPM_PROFILE: ['gcc5-armv7']
      EPM_SCHEME: ['shared', 'static']

    # =========================================
    # gcc7-armv8 armv8 build
    # =========================================
libwebsockets:gcc7-armv8@build:
  stage: build
  tags:
  - Linux
  - builder
  - docker
  - workbench:oss-0.2
  image: 172.16.0.119:8482/edgetoolkit/gcc7-armv8
  artifacts:
    paths:
    - libwebsockets/.conan/$EPM_PROFILE/$EPM_SCHEME
    - libwebsockets/.epm
  script:
  - cd libwebsockets
  - epm create --archive .conan/$EPM_PROFILE/$EPM_SCHEME
  parallel:
    matrix:
    - EPM_PROFILE: ['gcc7-armv8']
      EPM_SCHEME: ['shared', 'static']

    # =========================================
    # gcc5 x86_64 build
    # =========================================
libwebsockets:gcc5@build:
  stage: build
  tags:
  - Linux
  - builder
  - docker
  - workbench:oss-0.2
  image: 172.16.0.119:8482/edgetoolkit/gcc5
  artifacts:
    paths:
    - libwebsockets/.conan/$EPM_PROFILE/$EPM_SCHEME
    - libwebsockets/.epm
  script:
  - cd libwebsockets
  - epm create --archive .conan/$EPM_PROFILE/$EPM_SCHEME
  parallel:
    matrix:
    - EPM_PROFILE: ['gcc5', 'gcc5d']
      EPM_SCHEME: ['shared', 'static']

    # =========================================
    # gcc7 x86_64 build
    # =========================================
libwebsockets:gcc7@build:
  stage: build
  tags:
  - Linux
  - builder
  - docker
  - workbench:oss-0.2
  image: 172.16.0.119:8482/edgetoolkit/gcc7
  artifacts:
    paths:
    - libwebsockets/.conan/$EPM_PROFILE/$EPM_SCHEME
    - libwebsockets/.epm
  script:
  - cd libwebsockets
  - epm create --archive .conan/$EPM_PROFILE/$EPM_SCHEME
  parallel:
    matrix:
    - EPM_PROFILE: ['gcc7d', 'gcc7']
      EPM_SCHEME: ['shared', 'static']

    # =========================================
    # gcc5-armv8 armv8 build
    # =========================================
libwebsockets:gcc5-armv8@build:
  stage: build
  tags:
  - Linux
  - builder
  - docker
  - workbench:oss-0.2
  image: 172.16.0.119:8482/edgetoolkit/gcc5-armv8
  artifacts:
    paths:
    - libwebsockets/.conan/$EPM_PROFILE/$EPM_SCHEME
    - libwebsockets/.epm
  script:
  - cd libwebsockets
  - epm create --archive .conan/$EPM_PROFILE/$EPM_SCHEME
  parallel:
    matrix:
    - EPM_PROFILE: ['gcc5-armv8']
      EPM_SCHEME: ['shared', 'static']

    # =========================================
    # gcc6-armv8 armv8 build
    # =========================================
libwebsockets:gcc6-armv8@build:
  stage: build
  tags:
  - Linux
  - builder
  - docker
  - workbench:oss-0.2
  image: 172.16.0.119:8482/edgetoolkit/gcc6-armv8
  artifacts:
    paths:
    - libwebsockets/.conan/$EPM_PROFILE/$EPM_SCHEME
    - libwebsockets/.epm
  script:
  - cd libwebsockets
  - epm create --archive .conan/$EPM_PROFILE/$EPM_SCHEME
  parallel:
    matrix:
    - EPM_PROFILE: ['gcc6-armv8']
      EPM_SCHEME: ['shared', 'static']

    # =========================================
    # gcc6-armv7 armv7 build
    # =========================================
libwebsockets:gcc6-armv7@build:
  stage: build
  tags:
  - Linux
  - builder
  - docker
  - workbench:oss-0.2
  image: 172.16.0.119:8482/edgetoolkit/gcc6-armv7
  artifacts:
    paths:
    - libwebsockets/.conan/$EPM_PROFILE/$EPM_SCHEME
    - libwebsockets/.epm
  script:
  - cd libwebsockets
  - epm create --archive .conan/$EPM_PROFILE/$EPM_SCHEME
  parallel:
    matrix:
    - EPM_PROFILE: ['gcc6-armv7']
      EPM_SCHEME: ['shared', 'static']

    # =================================
    # MSVC test
    # =================================
libwebsockets:MSVC@test:
  stage: test
  tags:
  - Windows
  - runner
  - workbench:oss-0.2
  dependencies:
  - libwebsockets:MSVC@build
  script: |
    cd libwebsockets
    $EPM_SANDBOX_STORAGE = ".conan/$EPM_PROFILE/$EPM_SCHEME" 
        
    .epm/$EPM_PROFILE@$EPM_SCHEME/sandbox/test_package
    
  parallel:
    matrix:
    - EPM_PROFILE: ['vs2019d', 'vs2019']
      EPM_SCHEME: ['shared', 'static']


    # =========================================
    # gcc6 x86_64 test
    # =========================================
libwebsockets:gcc6@test:
  stage: test
  tags:
  - Linux
  - runner
  - docker
  - workbench:oss-0.2
  image: edgetoolkit/ubuntu:xenial
  dependencies:
  - libwebsockets:gcc6@build
  script:
    cd libwebsockets
    export EPM_SANDBOX_STORAGE = ".conan/$EPM_PROFILE/$EPM_SCHEME"    
        
    .epm/$EPM_PROFILE@$EPM_SCHEME/sandbox/test_package
    
    
  parallel:
    matrix:
    - EPM_PROFILE: ['gcc6d', 'gcc6']
      EPM_SCHEME: ['shared', 'static']








    # =========================================
    # gcc5 x86_64 test
    # =========================================
libwebsockets:gcc5@test:
  stage: test
  tags:
  - Linux
  - runner
  - docker
  - workbench:oss-0.2
  image: edgetoolkit/ubuntu:xenial
  dependencies:
  - libwebsockets:gcc5@build
  script:
    cd libwebsockets
    export EPM_SANDBOX_STORAGE = ".conan/$EPM_PROFILE/$EPM_SCHEME"    
        
    .epm/$EPM_PROFILE@$EPM_SCHEME/sandbox/test_package
    
    
  parallel:
    matrix:
    - EPM_PROFILE: ['gcc5', 'gcc5d']
      EPM_SCHEME: ['shared', 'static']


    # =========================================
    # gcc7 x86_64 test
    # =========================================
libwebsockets:gcc7@test:
  stage: test
  tags:
  - Linux
  - runner
  - docker
  - workbench:oss-0.2
  image: edgetoolkit/ubuntu:xenial
  dependencies:
  - libwebsockets:gcc7@build
  script:
    cd libwebsockets
    export EPM_SANDBOX_STORAGE = ".conan/$EPM_PROFILE/$EPM_SCHEME"    
        
    .epm/$EPM_PROFILE@$EPM_SCHEME/sandbox/test_package
    
    
  parallel:
    matrix:
    - EPM_PROFILE: ['gcc7d', 'gcc7']
      EPM_SCHEME: ['shared', 'static']







    # =================================
    # Deploy to conan repo
    # =================================
libwebsockets@deploy:
  stage: deploy
  tags:
  - conan
  - deployer
  - workbench:oss-0.2
  dependencies:
  - libwebsockets:MSVC@build
  - libwebsockets:gcc6@build
  - libwebsockets:gcc7-armv7@build
  - libwebsockets:gcc5-armv7@build
  - libwebsockets:gcc7-armv8@build
  - libwebsockets:gcc5@build
  - libwebsockets:gcc7@build
  - libwebsockets:gcc5-armv8@build
  - libwebsockets:gcc6-armv8@build
  - libwebsockets:gcc6-armv7@build
  script: |
    set -e
    cd libwebsockets
    PROFILES=(
      vs2019d vs2019
      gcc6d gcc6
      gcc7-armv7
      gcc5-armv7
      gcc7-armv8
      gcc5 gcc5d
      gcc7d gcc7
      gcc5-armv8
      gcc6-armv8
      gcc6-armv7
    )

    for profile in ${PROFILES[*]}
    do
      for scheme in shared static
      do
        export EPM_PROFILE=$profile
        export EPM_FPROFILE=$scheme
        echo [libwebsockets] - Upload...  $profile $shcme
        epm upload --storage .conan/$profile/$scheme
      done
    done 