
stages:
- build
- deploy


    # =================================
    # MSVC build
    # =================================
protoc:MSVC@build:
  stage: build
  tags:
  - Windows
  - builder
  - workbench:oss-0.2
  artifacts:
    paths:
    - protoc/.conan/$EPM_PROFILE/$EPM_SCHEME
    - protoc/.epm
  script:
  - cd protoc
  - epm create --archive .conan/$EPM_PROFILE/$EPM_SCHEME
  parallel:
    matrix:
    - EPM_PROFILE: ['vs2019']
      EPM_SCHEME: ['None']

    # =========================================
    # gcc5 x86_64 build
    # =========================================
protoc:gcc5@build:
  stage: build
  tags:
  - Linux
  - builder
  - docker
  - workbench:oss-0.2
  image: 172.16.0.119:8482/edgetoolkit/gcc5
  artifacts:
    paths:
    - protoc/.conan/$EPM_PROFILE/$EPM_SCHEME
    - protoc/.epm
  script:
  - cd protoc
  - epm create --archive .conan/$EPM_PROFILE/$EPM_SCHEME
  parallel:
    matrix:
    - EPM_PROFILE: ['gcc5']
      EPM_SCHEME: ['None']

    # =================================
    # Deploy to conan repo
    # =================================
protoc@deploy:
  stage: deploy
  tags:
  - conan
  - deployer
  - workbench:oss-0.2
  dependencies:
  - protoc:gcc5@build
  - protoc:MSVC@build
  script: |
    set -e
    cd protoc
    PROFILES=(
      gcc5
      vs2019
    )

    for profile in ${PROFILES[*]}
    do
      for scheme in None
      do
        export EPM_PROFILE=$profile
        export EPM_FPROFILE=$scheme
        echo [protoc] - Upload...  $profile $shcme
        epm upload --storage .conan/$profile/$scheme
      done
    done 