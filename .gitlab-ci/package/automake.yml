
stages:
- build
- deploy


    # =================================
    # MSVC build
    # =================================
automake:MSVC@build:
  stage: build
  tags:
  - Windows
  - builder
  - workbench:Z1
  artifacts:
    paths:
    - automake/.conan/$EPM_PROFILE/$EPM_SCHEME
    - automake/.epm
  script:
  - cd automake
  - epm create --archive .conan/$EPM_PROFILE/$EPM_SCHEME   --clear
  parallel:
    matrix:
    - EPM_PROFILE: ['vs2019']
      EPM_SCHEME: ['None']

    # =========================================
    # gcc5 x86_64 build
    # =========================================
automake:gcc5@build:
  stage: build
  tags:
  - Linux
  - builder
  - docker
  - workbench:Z1
  image: 172.16.0.119:8482/edgetoolkit/gcc5
  artifacts:
    paths:
    - automake/.conan/$EPM_PROFILE/$EPM_SCHEME
    - automake/.epm
  script:
  - cd automake
  - epm create --archive .conan/$EPM_PROFILE/$EPM_SCHEME  --clear
  parallel:
    matrix:
    - EPM_PROFILE: ['gcc5']
      EPM_SCHEME: ['None']

    # =================================
    # Deploy to conan repo
    # =================================
automake@deploy:
  stage: deploy
  tags:
  - conan
  - deployer
  - workbench:Z1

  image: 172.16.0.119:8482/edgetoolkit/gcc5

  dependencies:
  - automake:gcc5@build
  - automake:MSVC@build
  script:
  - epm -p gcc5 -s None upload --storage .conan/gcc5/None
  - epm -p vs2019 -s None upload --storage .conan/vs2019/None