name: c-ares
version: 1.15.0

scheme:
  shared:
    options:
      shared: true

  static:
    options:
      shared: false

test:
  test_package:
    project: test_package
    program: example
