name: brotli
version: 1.0.9

scheme:
  shared:
    options:
      shared: true
  static:
    options:
      shared: false

program:
- name: test_package
  location: test_package
  executable: bin/test_package

# CI: 4