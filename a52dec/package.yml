name: a52dec
version: 0.7.4


scheme:
  shared:
    options:
      shared: true
  static:
    options:
      shared: false

program:
- name: a52dec
#  location: test_package
  executable: bin/a52dec

