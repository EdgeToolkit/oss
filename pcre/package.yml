name: pcre
version: 8.44

scheme:
  shared:
    options:
      shared: true
  static:
    options:
      shared: false

dependencies:
  zlib: 
    version: 1.2.11
    if: options.with_zlib
  bzip2: 
    version: 1.0.8
    if: options.with_bzip2  

test:
  pcre:
    project: test_package
    program: test_package

# CI 1  