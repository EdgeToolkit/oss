{% if platform == 'Linux' %}
[[runners]]
  name = "Linux builder"
  url = "{{url}}"
  token = "{{token}}"
  executor = "docker"
  pre_clone_script = "sudo chown -R $(id -u):$(id -g) /workbench && ln -s /workbench $HOME/.epm"
  pre_build_script = "cmod +x $HOME/.epm/script/pre_build_script.py && $HOME/.epm/script/pre_build_script.py"
  environment = ["LC_ALL=en_US.UTF-8"]
  [runners.docker]
    tls_verify = false
    image = "None"
    privileged = false
    disable_cache = false
    volumes = ["/cache", "{{HOME}}/.edgetoolkit/gitlab-runner/{{workbench}}/builder:/workbench"]
    pull_policy = "if-not-present"
    shm_size = 0
  [runners.cache]
{% endif %}

{% if platform == 'Windows' %}
{% endif%}