name: libtool
version: 2.4.6

#scheme:
#  shared:
#    options:
#      shared: true
#
#  static:
#    options:
#      shared: false

build-tools:
    msys2:
        version: 20190524
        if: os == Windows

dependencies:
    automake: 1.16.2
