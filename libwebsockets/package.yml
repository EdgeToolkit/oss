name: libwebsockets
version: 4.0.15

scheme:
  shared:
    options:
      shared: true
  static:
    options:
      shared: false

sandbox:
   test_package: test_package/bin/test_package

dependencies:
  openssl: 
    version: 1.1.1g
    if: options.with_ssl == openssl
  sqlite3:
    version: 
    if: options.with_sqlite3
