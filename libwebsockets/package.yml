name: libwebsockets
version: 4.1.6

scheme:
  shared:
    options:
      shared: true
  static:
    options:
      shared: false

dependencies:
  openssl: 
    version: 1.1.1i
    if: options.with_ssl == openssl
  sqlite3:
    version: 3.35.4
    if: options.with_sqlite3
    
test:
  test_package:
    project: test_package

# CI 2