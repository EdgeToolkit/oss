name: protobuf
version: 3.12.4

scheme:
  shared:
    options:
      shared: true
  static:
    options:
      shared: false

sandbox:
   test_package: test_package/bin/test_package

build-tools:
  protoc:
    version: ${__version__}
    if: cross_build

dependencies:
  zlib: 
    version: 1.2.11
    if: options.with_zlib

#
