name: protobuf
version: 3.15.5

scheme:
  shared:
    options:
      shared: true
  static:
    options:
      shared: false

build-tools:
  protoc:
    version: ${__version__}
    if: cross_build

dependencies:
  zlib: 
    version: 1.2.11
    if: options.with_zlib



test:
  test_package:
    project: test_package


#####

